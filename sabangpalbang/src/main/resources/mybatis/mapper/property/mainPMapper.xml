<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.app.main.dao.PDAO">
	<!-- Property 매물 정보 -->
	<select id="getProperties" resultType="com.app.dto.PropertyDTO">
		SELECT
		property_id,
		p_service_type,
		user_id,
		property_type_id,
		price,
		feet,
		pname,
		deposit,
		charge,
		count,
		registration_date,
		move_date,
		address,
		description,
		floor
		FROM property
	</select>
</mapper>